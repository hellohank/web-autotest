· 整个配置只有两部分组成：case与data
· 以每个data组成一个真正执行的用例。
· 用例中的命令逐行执行，但整个用例在读取的时候是一次性读取并解析的！
· 用例中操作命令都是可扩展的！
· 每个操作命令对应一个命令的实现，其中分支命令是一个分块式的命令集！

· 在定义用例时，如果遇到分支时，根据输入的分支内容自动补全分支流程。
· 在组织数据时，如果遇到分支，提供下拉框选择，当选择后，只显示且要求填写被选择的模块部分。
· 在展示时，如果有分支的情况，则在被选择前，分支不展示！
· 数据定义时，只能决定哪些步骤填不填写数据，而不能决定步骤的顺序等。
